package fullWellFormedness;

import java.util.Map;

import fearlessFullGrammar.TName;
import utils.Bug;

public class ApplyUses {
  Package applyUses(Package p, Map<TName,String> uses){ throw Bug.todo(); }
}
